services:
  web:
    image: capetown-web:${IMAGE_TAG:-latest}
    build: .
    restart: unless-stopped
    mem_limit: ${WEB_MEM_LIMIT:-768m}
    memswap_limit: ${WEB_MEM_LIMIT:-768m}
    cpus: ${WEB_CPUS:-1.0}
    ports:
      - "${WEB_BIND:-0.0.0.0}:${WEB_PORT:-3000}:3000"
    env_file: .env
    environment:
      DATABASE_URL: "file:/app/data/site.db"
      UPLOAD_DIR: "/app/storage/uploads"
    volumes:
      - ./data:/app/data            # SQLite database
      - ./storage/uploads:/app/storage/uploads   # uploaded images & videos
